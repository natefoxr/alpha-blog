<h1 class="text-center mt-4 fw-bold">Category: <%= @category.name %></h1>

<% if logged_in? && current_user.admin? %>
  <div class="text-center mt-4">
    <%= link_to 'Edit Category Name', edit_category_path(@category), class: "btn btn-light text-primary border-primary bg-transparent fw-bold shadow" %>
  </div>
<% end %>

<%= will_paginate @articles, class: "flickr_pagination mt-3" %>
<%= render 'articles/article' %>
<%= will_paginate @articles, class: "flickr_pagination mb-5" %>